# Complete dependency list for the `vllm-bench-gui` conda env.
# Nothing is taken from the system Python; the only system assumptions are
# `apptainer` (vLLM runs from the bundled .sif) and, on a cluster, SLURM.
#
#   conda create -y -n vllm-bench-gui python=3.12
#   conda activate vllm-bench-gui
#   pip install -r requirements.txt

# Web backend
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6

# vLLM /health polling and any HTTP the backend does
httpx>=0.27

# Model downloads. app/services/downloader.py shells out to `hf download`
# (falling back to `huggingface-cli`), so the env must provide that command:
# hub 1.x ships it in the base package — there is no [cli] extra any more.
huggingface_hub>=1.0

# Frontend needs no packages (no build step); ApexCharts is vendored as a
# static file under static/vendor/.
